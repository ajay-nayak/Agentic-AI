[project]
name = "02-model-switching-groq"
version = "0.1.0"
description = "Multi-model switching, comparison, and provider routing with Groq & LangChain."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.2.15",
    "langchain-core>=0.3.0",
    "langchain-groq>=0.2.0",
    "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
